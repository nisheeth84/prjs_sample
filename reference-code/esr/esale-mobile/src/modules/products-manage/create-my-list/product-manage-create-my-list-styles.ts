import { StyleSheet } from "react-native";
import { theme } from "../../../config/constants";

export const CreateMyListStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: theme.colors.gray100,
  },
});
