import { theme } from "../../../config/constants";
import { StyleSheet } from "react-native";

export const CreateEditShareListStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: theme.colors.gray100,
  },
});
