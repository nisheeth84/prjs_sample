import React from "react";

/**
 * Component for show lookup fields
 * @param props see IFieldDetailLookupProps
 */
export function FieldDetailLookup() {
  /**
   * Render main component
   */
  const renderComponent = () => {
    return (
      <></>
    )
  };
  return renderComponent();
}