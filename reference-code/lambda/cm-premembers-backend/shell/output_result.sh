#!/bin/bash
cd `dirname $0` || exit 1
. ./args.conf
cat <<END > ./result.txt
S3_REPORT_BUCKET=${S3_REPORT_BUCKET}
SNS_ORGANIZATION_TOPIC=${SNS_ORGANIZATION_TOPIC}
COGNITO_USER_POOL_ID=${COGNITO_USER_POOL_ID}
SQS_ORGANIZATION_QUEUE=${SQS_ORGANIZATION_QUEUE}
GIT_HASH=${CODEBUILD_SOURCE_VERSION}
END